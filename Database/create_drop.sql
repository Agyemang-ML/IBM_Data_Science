CREATE TABLE Test (
    ID integer,
    Name VARCHAR(30)
);

create table COUNTRY (
ID integer,
CCODE char(2),
NAME varchar(30)
);

create table COUNTRY (
ID int not NULL,
CCODE char(2),
NAME varchar(60),
primary key (ID)
);

